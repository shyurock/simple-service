plugins {
    id 'org.siouan.frontend-jdk21' version '10.0.0'
    id 'java'
}

frontend {
    nodeVersion = '22.14.0'
    checkScript = 'run check'
    assembleScript = 'run build-only'
    verboseModeEnabled = true
}

jar {
    dependsOn 'assembleFrontend'
    from 'dist'
    into 'static'
}